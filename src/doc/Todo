Todo

open issues for version 0.1.0
=============================
- svn commands:
  + remove
  
- questions before make a svn command without FileSelector
- use FileSelector for remove, add
- recursive remove, revert, add


open issues
===========
- svn commands:
  + copy
  + mkdir
  + move

- WorkingCopy:
  + save and restore last changed item on quit and start
  + context menu
  
- WorkingCopyItem
  + stati if the directory can update, commit, etc. pp.

- FileList:
  + revision number
  + last editor
  + timestamp
  + conext menu

- FileListItem
  + fullpath 
  + filename

- settings:
  + change action on double click in FileList and WorkingCopy

- new dialogs:
  + repository browser
  + property editor
  + log viewer
    + search in logs
  
- eye candy:
  + include pixmaps for actions
