Todo

open issues for version 0.1.0
=============================
- save & restore working copys & layout
- File List View with status
- remove WorkingCopy
- svn checkout
- svn update
- svn commit
- svn add
- svn remove


open issues
===========
- Repository Browser
- svn mkdir
- svn move
- svn copy
- FileList Enhancements: revision number, last editor, timestamp
- include pixmaps for file status and cvs actions
- search in log's from a given directory. optionally recursive

ideas
=====
- WorkingCopyItem based on QListViewItem with:
  * full path
  * stati if the directory can update, commit, etc. pp.
- FileListItem based on QListViewItem with:
  * full path
